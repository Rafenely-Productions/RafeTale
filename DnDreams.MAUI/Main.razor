@using Microsoft.AspNetCore.Components.Routing
@using DnDreams.UI.Shared.Pages
@using DnDreams.UI.Shared.Shared

<Router AppAssembly="@typeof(Main).Assembly"
        AdditionalAssemblies="new[] { typeof(DnDreams.UI.Shared.Pages.SandboxPage).Assembly }">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(DnDreams.UI.Shared.Shared.MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <LayoutView Layout="@typeof(DnDreams.UI.Shared.Shared.MainLayout)">
            <p role="alert">No hay nada aquí. Parece que fallaste tu tirada de Percepción.</p>
        </LayoutView>
    </NotFound>
</Router>