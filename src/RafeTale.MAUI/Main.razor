@using Microsoft.AspNetCore.Components.Routing

<Router AppAssembly="@typeof(Main).Assembly"
        AdditionalAssemblies="new[] { typeof(RafeTale.UI.Shared.Pages.SandboxPage).Assembly }">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(RafeTale.UI.Shared.Shared.MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <LayoutView Layout="@typeof(RafeTale.UI.Shared.Shared.MainLayout)">
            <p role="alert">No hay nada aquí. Parece que fallaste tu tirada de Percepción.</p>
        </LayoutView>
    </NotFound>
</Router>

